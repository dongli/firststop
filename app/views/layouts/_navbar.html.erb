<nav class='navbar navbar-default'>
  <div class='container-fluid'>
    <div class='navbar-header'>
      <button type='button' class='navbar-toggle collapsed' data-toggle='collapse' data-target='#navbar-items' aria-expanded='false'>
        <span class='sr-only'>Toggle navigation</span>
        <span class='icon-bar'></span>
        <span class='icon-bar'></span>
        <span class='icon-bar'></span>
      </button>
      <%= link_to t('firststop'), root_path, class: 'navbar-brand' %>
    </div>
    <div class='collapse navbar-collapse' id='navbar-items'>
      <ul class='nav navbar-nav navbar-right'>
        <% if user_signed_in? %>
          <li class='dropdown'>
            <a class='dropdown-toggle' data-toggle='dropdown' role='button'
              aria-haspopup='true' aria-expanded='false'>
              <%= image_tag current_user.avatar.url(:small), class: 'avatar' %>
            </a>
            <ul class='dropdown-menu'>
              <li><%= link_to fa_icon('home', text: t('navbar.home')), current_user %></li>
              <li><%= link_to fa_icon('edit', text: t('navbar.profile')), edit_user_path(current_user) %></li>
              <li><%= link_to fa_icon('dashboard', text: t('navbar.dashboard')), carload.dashboard_index_path %></li>
              <li><%= link_to fa_icon('sign-out', text: t('action.sign_out')), destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to t('action.sign_up'), new_user_registration_path %></li>
          <li><%= link_to t('action.sign_in'), new_user_session_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
